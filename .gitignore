# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
/.idea/

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — 실제 값이 담긴 파일은 절대 커밋하지 않는다
.env*
# 단, 값이 비어 있는 템플릿 파일은 커밋 허용
!.env.example

# ── 개발자 전용 파일 (공개 레포에 올라가면 안 되는 것들) ──────────────────────
# 디자인 시스템 내부 문서
DESIGN.md
# Claude Code AI 어시스턴트 설정 — 로컬 머신 경로 포함
.claude/
# Claude Code 에이전트 스킬 파일
.agents/
# AI 협업 개발 가이드라인 — 내부 컨벤션 및 개발자 자기 서술 포함
CLAUDE.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
